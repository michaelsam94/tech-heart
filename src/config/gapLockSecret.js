/**
 * Optional: paste ONLY the SHA-256 hex digest (64 lowercase hex chars) of your
 * 12-character unlock passphrase. Used when `VITE_GAP_UNLOCK_SHA256` is not set.
 * Never commit the raw passphrase—only this hash.
 */
export const GAP_UNLOCK_SHA256_FALLBACK = ''
